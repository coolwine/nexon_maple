{
  "name": "event",
  "version": "1.0.0",
  "description": "Event & Reward",
  "main": "index.js",
  "scripts": {
    "start:docker": "docker compose up --build",
    "start:dev": "docker compose up -d",
    "stop": "docker compose down",
    "build": "docker compose build",
    "test:e2e": "cd test && npm run test:e2e",
    "install:all": "npm install && cd test && npm install && cd ../gateway && npm install && cd ../auth && npm install && cd ../event && npm install",
    "dev": "docker-compose up -d && echo Services started! Access the API at http://localhost:3000"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
